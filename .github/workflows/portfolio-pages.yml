name: Deploy portfolio and SUWENKU

on:
  push:
    branches: [main]
    paths:
      - 'portfolio/**'
      - 'suwenku/**'
      - '.github/workflows/portfolio-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820024
        with:
          node-version: 22
      - name: Run portfolio smoke test
        run: node portfolio/smoke-test.mjs
      - name: Validate SUWENKU
        run: |
          test -f suwenku/index.html
          test -f suwenku/styles.css
          test -f suwenku/app.js
          grep -q 'SUWENKU' suwenku/index.html

  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Setup Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b
      - name: Prepare static site
        run: |
          mkdir -p _site/suwenku
          cp -R portfolio/. _site/
          cp -R suwenku/. _site/suwenku/
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e