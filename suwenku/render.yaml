services:
  - type: web
    name: momoha-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGIN
        sync: false
      - key: DATA_DIR
        value: /app/api/data
