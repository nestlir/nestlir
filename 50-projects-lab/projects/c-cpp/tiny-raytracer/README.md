# Tiny Ray Tracer (C++17)

A minimal ray tracer that renders a sphere into a PPM image.

## Build
```bash
make
./raytracer
```

The program writes `image.ppm`, which can be opened by many image viewers or converted with standard image tools.