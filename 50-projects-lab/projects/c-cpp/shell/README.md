# Mini Shell (C)

A small POSIX shell built from scratch.

## Features
- interactive prompt
- `fork` + `execvp`
- tokenized arguments
- built-ins: `cd`, `pwd`, `exit`

## Build
```bash
make
./mini-shell
```

## Learning goals
Processes, process replacement, PATH lookup and waiting for child processes.