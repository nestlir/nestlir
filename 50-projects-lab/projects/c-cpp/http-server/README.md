# Tiny HTTP Server (C)

A minimal single-process HTTP/1.1 server.

## Build and run

```bash
make
./tiny-http-server
```

Then open `http://localhost:8080/`.

## Learning goals

Sockets, bind/listen/accept, TCP connections and HTTP response formatting.