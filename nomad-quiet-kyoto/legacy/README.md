# Legacy NOMAD iterations

This directory is reserved for historical prototypes and intermediate refactors.

Do not import from `legacy/` into the canonical runtime.

The files are preserved so visual and behavioral regressions can be compared during migration.
